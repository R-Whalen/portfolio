/**
 * Dentoes a container of work with all associated required information for browsing
 */
export interface Project {
    title: string,
    description: string,
    image: string,
    link: string,
    tags: string[]
};